# Core orchestration
langgraph>=0.2.40
langchain-core>=0.3.0
langchain-anthropic>=0.2.0      # LLM reasoning (optional at runtime — see config)

# Async HTTP + resilience
aiohttp>=3.9.0
tenacity>=8.2.0                 # retry/backoff for API limits & timeouts

# Data modeling + config
pydantic>=2.6.0
python-dotenv>=1.0.0
